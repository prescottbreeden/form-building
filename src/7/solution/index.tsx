import { CreateSettings } from "./CreateSettings"

// This is not a working solution. But if necessary it might prove useful for 
// getting some ideas about how you might want to tackle it if you are having
// writer's block
export const Solution = () => {
  return <CreateSettings />
}
